<h1>Pictures/index</h1>
<li>Pictures: <%= @pictures.count %></li>
<%== pagy_bootstrap_nav(@pagy) %>
<%= "===========================================" %>
<% 2.times { %><br>
<% } %>
<div class="container">
  <div class="row">
    <% @records.each do |aaa| %>
      <div class="col-sm m-1 text-center">
        <%= link_to image_tag(aaa.variant(resize_to_limit: [100, 100])), picture_path(aaa) %>
      </div>
    <% end %>
  </div>
</div>

